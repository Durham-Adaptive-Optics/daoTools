name: Clone Test

on:
  push:
    branches: [ CI-Pipeline ]
  pull_request:
    branches: [ CI-Pipeline ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        run: echo "Hello world"

