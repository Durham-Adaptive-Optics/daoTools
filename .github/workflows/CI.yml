name: Clone Test

on:
  push:
    branches: [ CI-Workflow ]
  pull_request:
    branches: [ CI-Workflow ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        run: echo "Hello world"

