
import os
import platform

if platform.system() == "Darwin":
	my_ld_flags = ['']
	my_cxx_flags = ['-std=c++14']
	my_cflags = ['-O3', '-ffast-math']
else:
	my_ld_flags = ['-lrt']
	my_cxx_flags = ['']
	my_cflags = ['-Ofast']



daoRandWriterSync = bld.program(
	source = 'daoRandWriterSync.c',
	target = 'daoRandWriterSync',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao', '-lpthread', '-lm'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoClock = bld.program(
	source = 'daoClock.c',
	target = 'daoClock',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoTimeDiff = bld.program(
	source = 'daoTimeDiff.c',
	target = 'daoTimeDiff',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao'  , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoReadWrite = bld.program(
	source = 'daoReadWrite.c',
	target = 'daoReadWrite',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao'  , '-lpthread', '-lm'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoTimeDiffStat = bld.program(
	source = 'daoTimeDiffStat.c',
	target = 'daoTimeDiffStat',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao'  , '-lpthread', '-lm'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoCommandFilter = bld.program(
	source = 'daoCommandFilter.c',
	target = 'daoCommandFilter',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm'] + my_ld_flags,
	use = 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoLeakyIntegrator = bld.program(
	source = 'daoLeakyIntegrator.c',
	target = 'daoLeakyIntegrator',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm'] + my_ld_flags,
	use = 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

# daoDMSend = bld.program(
# 	source = 'daoDMSend.c',
# 	target = 'daoDMSend',
# 	includes = '../include '+os.getenv('DAOROOT')+'/include/',
# 	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
# 	ldflags = ['-ldao' , '-lpthread', '-lm'] + my_ld_flags,
# 	use = 'daoTools',
# 	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
# 	)

daoShmMonitoring = bld.program(
	source = 'daoShmMonitoring.c',
	target = 'daoShmMonitoring',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use = 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoShmMonitoring1Value = bld.program(
	source = 'daoShmMonitoring1Value.c',
	target = 'daoShmMonitoring1Value',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao'  , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoPixelCalibrate = bld.program(
	source = 'daoPixelCalibrate.c',
	target = 'daoPixelCalibrate',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoPixelCalibratePws = bld.program(
	source = 'daoPixelCalibratePws.c',
	target = 'daoPixelCalibratePws',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoShmCombiner = bld.program(
	source = 'daoShmCombiner.c',
	target = 'daoShmCombiner',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoShm2Fits = bld.program(
	source = 'daoShm2Fits.c',
	target = 'daoShm2Fits',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoComputeCentroid = bld.program(
	source = 'daoComputeCentroid.c',
	target = 'daoComputeCentroid',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoComputeCentroidRelative = bld.program(
	source = 'daoComputeCentroidRelative.c',
	target = 'daoComputeCentroidRelative',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-pipe', '-fdiagnostics-color=auto', '-g'],
	)

daoComputeCentroidPws = bld.program(
	source = 'daoComputeCentroidPws.c',
	target = 'daoComputeCentroidPws',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoSetLatency = bld.program(
	source = 'daoSetLatency.c',
	target = 'daoSetLatency',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoAvgShm = bld.program(
	source = 'daoAvgShm.c',
	target = 'daoAvgShm',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-Werror', '-pipe', '-fdiagnostics-color=auto'],
	)

daoDescrambleOcam2 = bld.program(
	source = 'daoDescrambleOcam2.c',
	target = 'daoDescrambleOcam2',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-pipe', '-fdiagnostics-color=auto'],
	)

daoComputeIntensityPws = bld.program(
	source = 'daoComputeIntensityPws.c',
	target = 'daoComputeIntensityPws',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-pipe', '-fdiagnostics-color=auto'],
	)

daoStatShm = bld.program(
	source = 'daoStatShm.c',
	target = 'daoStatShm',
	includes = '../include '+os.getenv('DAOROOT')+'/include/',
	linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
	ldflags = ['-ldao' , '-lpthread', '-lm', '-lncurses'] + my_ld_flags,
	use= 'daoTools',
	cflags = my_cflags + ['-Wall', '-Wextra', '-pipe', '-fdiagnostics-color=auto'],
	)

if bld.env.BLAS_AVAILABLE:
	daoMvM = bld.program(
		source = 'daoMvM.c',
		target = 'daoMvM',
		includes = '../include '+os.getenv('DAOROOT')+'/include/',
		linkflags = [ '-L'+os.getenv('DAOROOT')+'/lib/','-L'+os.getenv('DAOROOT')+'/lib64/'],
		ldflags = ['-ldao' , '-lpthread', '-lm', '-lncurses', '-lopenblas'] + my_ld_flags,
		use= 'daoTools',
		cflags = my_cflags + ['-Wall', '-Wextra', '-pipe', '-fdiagnostics-color=auto'],
		)
else:
    print("Skipping daoMvM build (BLAS not available).")

# Conditionally build daoMvMGPU if CUDA is available
if bld.env.CUDA_AVAILABLE:
	daoMvMGPU = bld.program(
		source = 'daoMvMGPU.c',
		target = 'daoMvMGPU',
		includes = ['../include', os.getenv('DAOROOT')+'/include/', '/usr/local/cuda/include/'],
		linkflags = ['-L'+os.getenv('DAOROOT')+'/lib/', '-L'+os.getenv('DAOROOT')+'/lib64/', '-L/usr/local/cuda/lib64/'],
		ldflags = ['-ldao', '-lpthread', '-lm', '-lcublas', '-lcudart'],
		use= 'daoTools',
		cflags = my_cflags + ['-Wall', '-Wextra', '-pipe', '-fdiagnostics-color=auto'],
		)
else:
    print("Skipping daoMvMGPU build (CUDA not available).")